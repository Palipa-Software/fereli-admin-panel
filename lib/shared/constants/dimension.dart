import 'package:flutter/material.dart';

class AppDimensions {
  AppDimensions._();
  static const paddingHorizontal = EdgeInsets.symmetric(horizontal: 16);
  static const projectpaddingHorizontal = EdgeInsets.symmetric(horizontal: 20);
}
